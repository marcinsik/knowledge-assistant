# knowledge-assistant/backend/requirements.txt
fastapi==0.111.0 # Upewnij się, że masz aktualną wersję FastAPI
uvicorn==0.30.1 # Upewnij się, że masz aktualną wersję Uvicorn
SQLAlchemy==2.0.30
sqlmodel==0.0.18 # Dodaj SQLModel
psycopg2-binary==2.9.9 # Dodaj sterownik PostgreSQL
pypdf==4.2.0 # Dodaj do ekstrakcji tekstu z PDF
sentence-transformers==2.7.0 # Dodaj do generowania embeddingów
numpy==1.26.4 # Często wymagany przez sentence-transformers
psycopg2-binary  # Sterownik PostgreSQL
python-dotenv    # Do ładowania zmiennych środowiskowych z pliku .env